{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability par défaut de Ro'eh — fenêtre principale",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
